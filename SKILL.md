---
name: b-prompt
description: Use when 用户说「优化这个提示词」；只优化提示词，不执行任务。
version: 1.0.0
author: 21671 (BJC)
license: MIT
platforms: [linux, macos, windows]
metadata:
  tags: [prompt-engineering, prompt-optimization, agent-agnostic]
---

# B-PROMPT · 提示词优化

把一个模糊、缺信息的提示词改写成目标明确、输入清晰、结构一目了然的提示词。**产出是文本，不是执行结果。**

适用任何支持 SKILL.md 的 AI agent（Claude Code / Cursor / pi / Hermes / 其它）。核心流程只产出文本，因此不依赖任何特定工具；宿主若具备「读取文件 / 检索网页 / 运行命令」能力，可用于勘察与验证，缺失也不影响使用。

来源融合：通用提示词优化骨架（四模块输出）+ Lyra Prompt Optimizer 的机制（提示词类型分流、优化级别、评分维度、迭代目标），详见 `references/lyra-rubric.md`。

## 两条铁律

1. **只优化，不执行。** 原提示词里描述的业务任务（写文件、删数据、发消息、跑脚本、下结论）一律不做。任务要执行，必须用户另行确认。
2. **不虚构关键事实。** 信息不足 → 允许合理假设，但假设必须显式写进「已做假设」。关键事实（文件名、API、数字、引文、版本号）编不出来就留占位符 + 追问。

## 触发条件（默认关，仅显式触发）

**只有用户明确要求时才启用本 skill。** 不要对普通指令自动出卡，不要主动提议「要不要我帮你优化成提示词」。

| 用户说的话 | 干什么 | 产出 |
|---|---|---|
| 「优化这个提示词」「帮我改提示词」「提示词优化」「优化一下下面这段」 | 模式 B | 严格四模块 |
| 「v2」「再改一版」「按 X 方向再优化」 | 模式 B 迭代 | 四模块 + 版本头行 |
| 「把我这句转成提示词」「出个提示词卡」「帮我写个提示词」 | 模式 A | 提示词卡（≤5 行）+ 停手等确认 |
| 「提示词模式：常开 / 关」 | 切档 | 一句确认，不解释 |

其余一切——事实问答、闲聊、任务派发、代码修改、跑脚本、查文件——**一律不走本 skill**，哪怕用户这句话写得再模糊、再像提示词素材。

「提示词模式：常开」是唯一例外：开启后每条消息先出卡再干活。默认不开启。

## 模式 A：自然语言 → 提示词卡

把用户随口一句话转成四要素卡片，出卡后**停手**等他确认。

固定形状（能一行说完就一行，绝不写成任务书）：

```
【提示词卡 · 待确认】
目标：把 <X> 变成 <Y>
输入：<手上已有什么 / 缺什么 → 默认按什么假设走>
产出：<文件路径 / 结论 / 命令>，格式 <…>
边界：<只改哪里；不装包、不推送、不删除、不碰 <…>>
→ 回「确认」/「改：<修改点>」/「直接干」
```

规则：

- **不重述原话**，只填四要素。目标写成可验收的动作，不写「帮忙看看」这类空动词。
- **每格给默认值**，让用户回两个字就能推进。缺的信息用「默认按 … 走」兜住，不要用开放式提问堵住整条链路。
- **不写默认前提**：先读代码再动手、禁删除、最小变更等属于用户常驻规则，写进卡里是废话。
- **出卡即停手**：同一轮不并行开工（除非卡里写明「边做边等」，或本次是纯只读勘察）。
- **不等于任务书**：任务书是确认之后的事，两者不合并。

常开模式下的例外（这些情形即使常开也不出卡）：纯问答/查事实、用户说「直接干」、上一张卡已确认后的续跑、单个文件内的明确小修、以及**用户正在回答你的关键节点提问**时。

## 模式 B：提示词优化

### 固定输出（严格，不多不少四个模块）

```
1. **问题诊断**：2-4 句话，说明原提示核心问题与优化思路。【评分 原 XX → 优后 XX】
2. **优化后提示词**：放代码块，可直接复制使用。
3. **已做假设**：逐条列出全部假设，无则填「无」。
4. **关键追问**：最多 3 条，每条 2-3 个可选选项。
```

模块 1 的评分后缀是唯一允许的结构性增补（来自 Lyra 的 score 机制）：0-100，只标维度缺口不解释。用户说「不要评分」就去掉。迭代版在标题行加 `v2`（如 `## v2 · 目标：enhance_clarity`），不新增模块。

### 内部步骤（不输出，自己走）

1. **定类型**：user prompt（一次性任务说明）还是 system prompt（角色与行为约束）？两者改法不同 —— 见 `references/lyra-rubric.md`。没说是哪种就按内容判，判不出就假设为 user prompt 并写进模块 3。
2. **定接收方**：这条提示词最终喂给哪个模型？不同模型写法偏好不同（见 rubric 第 5 节）。定不了就写模型无关版。
3. **定级别**：默认 `basic`（简洁）；只有用户要「详细/专业/能上生产」才升 `advanced` / `expert`。级别定义见 rubric。
4. **补缺口**：目标、输入、产出格式、验收标准、边界五格过一遍。缺的补进模块 3，不编。
5. **精删**：砍掉「尽量 / 可能 / 希望 / 等等 / 如果有的话」这类软词；形容词换成可判定的标准（「写得专业些」→「含 3 条带出处的结论，每条 ≤50 字」）。删重复、删背景赘述。
6. **自检**：五格齐否？模块之间自相矛盾否？优化后的文本里有没有原提示词中不存在的事实断言（有 → 删或降级为假设）？

### 任务复杂度匹配

- **简单任务**（单步、单产出）：优化后提示词 3-8 行，保持原语气，**别硬拆**。简单任务拆成 6 步 = 没优化。
- **复杂任务**（多产出 / 多阶段 / 需要验收）：拆成编号步骤，每步带**验收标准**和**输出格式**；补必要背景（谁用、给谁看、边界）。
- 判断线：原提示词里出现「同时 / 分别 / 先…再… / 全部 / 逐个 / 每周」这类信号 → 复杂，拆。

## 禁止事项

- ❌ 执行原提示词描述的业务任务 —— 本 skill 最常被违反的一条。看到「帮我删掉 X」→ 产出优化后的文本，不动手。
- ❌ 发明事实：文件名、命令、API、数据、引文、链接。
- ❌ 擅自在优化里扩范围（用户只要求 A，不要顺手加 B、C、D 的「顺便建议」）。
- ❌ 输出四个模块之外的东西（评分并入模块 1，迭代版本并入标题行，改进点清单合并进模块 1 的句子里）。
- ❌ 关键追问超过 3 条，或问自己能定的事（能定就别问，写进模块 3 的假设）。
- ❌ 敏感信息外泄：项目名、本地路径、内部架构、人名、凭据，不得写进要交给外部模型或子 agent 的提示词。

## Pitfalls

- **擅自出卡**：用户没说「优化提示词」就当普通指令干，不出卡、不提醒、不问「要不要先转成提示词」。
- **把「优化」做成「执行」**：读着读着就去干了。纪律：输出只含文本，最后一个动作是输出。
- **模式 A 卡太长**：写成任务书 → 用户每次都得读一遍长文，等于把确认变成负担。卡 ≤5 行。
- **过度拆分**：把简单任务摊成步骤，token 膨胀。
- **假设藏在正文里**：假设混在优化后提示词里，等于替用户拍了板他还看不见 → 一律进模块 3。
- **追问替代判断**：把「你来选」当成省事手段。用户常整体授权（「全按你推荐的来」）→ 给可选选项 + 推荐项排第一。

## Verification

- 触发判定：本次是否**显式**要求优化/转换提示词。没触发 → 本 skill 不参与，直接按用户要求干活。
- 模式 A：卡已输出、已停手、四要素齐、每格有默认值 → 才算完成。
- 模式 B：四个模块齐；代码块可直接复制（无占位符残留，或占位符用 `<…>` 显式标注）；假设全列；追问 ≤3 且各带 2-3 选项；优化后文本无新增事实断言。
- 通用：本次回复**没有**对原任务产生任何副作用（无文件改动、无命令执行、无外发）。

## 参考

- `references/lyra-rubric.md` — user/system 提示词改法、级别定义、评分五维、迭代目标枚举、接收方适配。
- `references/examples.md` — 实例：模式 A 出卡、简单任务（不硬拆）、复杂多阶段（拆步 + 验收）、system prompt 角色类。
- `INSTALL.md` — 各 agent 的落盘位置与同步方式。
